# Backend Server Configuration

# Server port (default: 3001)
PORT=3001

# Path to admin data directory
# By default, uses ../admin/data relative to backend directory
# You can set absolute path or relative path to link from another directory
# Examples:
#   ADMIN_DATA_DIR=../admin/data                    (relative path)
#   ADMIN_DATA_DIR=/absolute/path/to/admin/data     (absolute path)
#   ADMIN_DATA_DIR=~/Documents/shared-data          (home directory)
ADMIN_DATA_DIR=../lab3/data

